DATABASE_URL=postgresql://usuario:contraseña@localhost:5432/control_doc
PORT=3001

# Autenticación
JWT_SECRET=cambia-esto-por-un-secreto-largo-y-aleatorio
SUPERADMIN_EMAIL=soporte@cognify.institute
SUPERADMIN_PASSWORD=cambia-esta-contraseña
# Dominios corporativos permitidos para iniciar sesión (separados por coma)
AUTH_ALLOWED_DOMAINS=shouxin.com.pe,cognify.institute

# Frontend (opcional): URL del API si no se sirve desde el mismo origen
# VITE_API_URL=https://api.tu-dominio.com

# ---------------------------------------------------------------------------
# Autosincronización del Excel de documentos (SharePoint/OneDrive)
# ---------------------------------------------------------------------------
# Enlace de "compartir" de SharePoint del archivo (con ?e=...). Debe estar
# compartido como "cualquiera con el enlace". Si está vacío, la sync se apaga.
# SYNC_SHARE_URL=https://...sharepoint.com/:x:/g/personal/.../IQ...?e=XXXX
# Hoja(s) a sincronizar (por ahora solo Documentos).
# SYNC_SHEET=Documentos
# Organización (id) dueña de los documentos sincronizados. Vacío = owner/global.
# SYNC_ORG_ID=
# Token para el disparo manual: POST /api/sync con Authorization: Bearer <token>.
# SYNC_TOKEN=cambia-esto-por-un-token-largo-y-aleatorio
# Zona horaria de la ventana 08:00–18:00 (por defecto America/Lima).
# SYNC_TZ=America/Lima
